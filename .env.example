# Used by npm run db:up when Docker is selected.
POSTGRES_PASSWORD=philfida_local_change_me

# Local PostgreSQL connection used by both Next.js and Drizzle.
# Copy this file to .env and change the password if you use an installed PostgreSQL server.
DATABASE_URL=postgresql://philfida:philfida_local_change_me@127.0.0.1:5432/philfida_it
